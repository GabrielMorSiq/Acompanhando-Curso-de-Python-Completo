# Escreva um programa que calcule a soma de três variáveis e imprima o resultado na tela

n = input("Digite um número: ")
m = input("Digite um número: ")
p = input("Digite um número: ")

# Se não ouver a conversão das strings para int ou float
# As variáveis só serão concatenadas
print(n + m + p)
print(float(n) + float(m) + float(p))
